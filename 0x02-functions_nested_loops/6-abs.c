#include "main.h"

/**
 * _abs - Computes absolute value of integer
 *
 * Return: 0 Always
 */

int _abs(int c)
{
	if (c < 0)
	{
		return (c);
	}
	else if (c == 0)
		return (0);
	else
		return (c);
}
